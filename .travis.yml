language: php

sudo: false

php:
  - 7.1
  - 7.2

cache:
  directories:
    - $HOME/.composer/cache

before_script:
  - composer install --prefer-source --no-interaction --dev

script:
  - mkdir -p build/logs
  - composer coverage

after_success:
  - travis_retry php vendor/bin/php-coveralls
